# LangGraph adapter

This boundary will own graph definitions, checkpointing choices, and translation between
Testflight contracts and LangGraph state. The upstream package stays optional until an
experiment needs orchestration.

Install it with `uv sync --all-packages --extra langgraph`.
