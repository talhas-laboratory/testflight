# Query

Compile workspace, Brand, perspective, time, and access scope before retrieval. Search definitions
and evidence lexically first; use bounded hybrid or explicit graph traversal only when justified.
