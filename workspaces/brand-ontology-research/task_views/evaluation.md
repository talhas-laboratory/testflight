# Evaluation view

Evaluate candidates independently across schema, invariants, semantics, question coverage,
fixture behavior, usefulness, and provenance. Compare positive and counterexamples. Equal-authority
conflicts remain `hold`; a model confidence value cannot replace evidence or authority.

Use the `evaluation-schema` and `ontology-conflation` operators and record their versions.
