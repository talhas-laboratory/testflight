# Proposal view

Create a candidate definition using `artifacts/ontology-promotion-contract.yaml`. Include necessary
meaning, inclusion/exclusion rules, examples, counterexamples, endpoint constraints, sources or
fixture IDs, question coverage, alternatives, and a derivation receipt. Proposal status is never
accepted by default.
