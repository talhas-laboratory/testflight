[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "testflight-core"
version = "0.1.0"
description = "Stable integration contracts for Testflight"
requires-python = ">=3.13,<3.14"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/testflight_core"]
